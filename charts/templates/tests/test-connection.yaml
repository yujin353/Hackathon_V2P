apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kiin-frontend-new.fullname" . }}-test-connection"
  labels:
    {{- include "kiin-frontend-new.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kiin-frontend-new.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
